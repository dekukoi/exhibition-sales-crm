.DS_Store
.env
.env.*
!.env.example

# Vendored Claude Code skill packages (npx skills add mattpocock/skills) —
# tooling used to build this, not part of the deliverable.
.claude/
skills-lock.json

# Build output (adjust for your stack).
node_modules/
.next/
dist/
build/
coverage/
__pycache__/
.pytest_cache/
.venv/
target/

# Keep the source data in Git.
!data/
!data/**
